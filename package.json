{
  "name": "deckstream",
  "version": "1.0.0",
  "private": true,
  "description": "Controlador Virtual de Macros y Escenas para Streamers",
  "author": "Juan Pablo Ospina Restrepo",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel --stream dev",
    "build": "pnpm -r build",
    "build:server": "pnpm --filter @deckstream/server build",
    "build:admin": "pnpm --filter @deckstream/admin build",
    "build:client": "pnpm --filter @deckstream/client build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "package": "pnpm --filter @deckstream/server package"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
